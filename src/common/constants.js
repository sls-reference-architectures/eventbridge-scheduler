const SERVICE_NAME = 'sls-ra-eventbridge-scheduler';
const ONE_TIME_SCHEDULE_CODE = 'OT';
const MAX_LIMIT = 100;

const ScheduleType = {
  ONE_TIME: 'ONE_TIME',
};

export { MAX_LIMIT, ONE_TIME_SCHEDULE_CODE, ScheduleType, SERVICE_NAME };
